{
  "id": "clawkeeper",
  "name": "ClawKeeper",
  "description": "Tasks and habits that live in a plain markdown file on your machine. Free, private, and claw-native: your AI assistant can read, plan, and act on your to-do list without an API or cloud sync.",
  "version": "0.2.0",
  "skills": ["./skills/clawkeeper"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "dataDir": {
        "type": "string",
        "description": "Path to ClawKeeper data directory (sets CLAWKEEPER_DIR)"
      }
    }
  },
  "uiHints": {
    "dataDir": {
      "label": "Data directory",
      "placeholder": "~/.clawkeeper"
    }
  }
}
